<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= name %> — Species</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container my-5">
    <div class="row">
      <div class="col-12 col-md-6 mb-4 mb-md-0">
        <img src="<%= image %>" class="img-fluid rounded" alt="<%= name %>">
      </div>
      <div class="col-12 col-md-6">
        <h2 class="template-text"><%= name %> <% if (scientificName) { %> (<%= scientificName %>) <% } %></h2>
        <ul class="list-group list-group-flush">
          <% if (region) { %>
          <li class="list-group-item list-template"><strong>Region:</strong> <%= region %></li>
          <% } %>
          <% if (habitat) { %>
          <li class="list-group-item list-template"><strong>Habitat:</strong> <%= habitat %></li>
          <% } %>
          <% if (threats) { %>
          <li class="list-group-item list-template"><strong>Threats:</strong> <%= threats %></li>
          <% } %>
          <% if (status) { %>
          <li class="list-group-item list-template"><strong>Conservation Status:</strong> <%= status %></li>
          <% } %>
        </ul>
        <p class="mt-3"><%= description || '' %></p>
        <a href="/catalog" class="btn btn-secondary mt-3">Go Back</a>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
